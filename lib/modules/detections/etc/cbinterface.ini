[production]

# Comma-separated list of companies to ignore from running the cbinterface searches
ignore_these_companies = 

# Comma-separated list of process names to ignore from the cbinterface searches
ignore_these_process_names = 

# Comma-separated list of process MD5s to ignore from the cbinterface searches
ignore_these_process_md5s = 
