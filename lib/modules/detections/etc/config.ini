[clickers]
# Comma-separated list of terms to exclude from the Splunk searches
whitelisted_things = 

# Valid regex statement that matches what your employee IDs look like
employee_id_pattern = 

# Comma-separated list of companies to ignore from running the Splunk searches
ignore_these_companies = 
